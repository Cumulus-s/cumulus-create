{
  "name": "@cumulus/db",
  "version": "0.1.0",
  "description": "HTTP SDK for Cumulus DB.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "prebuild": "npm --workspace @cumulus_cloud/events run build",
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm --workspace @cumulus_cloud/events run build",
    "test": "vitest run src"
  },
  "keywords": [
    "cumulus",
    "agent-db",
    "database",
    "sdk"
  ],
  "license": "Apache-2.0",
  "homepage": "https://cumulush.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Cumulus-s/cumulus-create.git",
    "directory": "packages/db-sdk"
  },
  "bugs": {
    "url": "https://github.com/Cumulus-s/cumulus-create/issues"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@cumulus_cloud/events": "0.1.0"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.4"
  }
}
